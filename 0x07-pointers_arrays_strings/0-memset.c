#include "main.h"
/*
 * _memset() - fills n bytes of memory pointed by s with the constant b
 * @b: constant byte b
 * @s: pointer of memory area *s
 *
 * Return: *s
 */
char *_memset(char *s, char b, unsigned int n);
{
	_memset(*s, b, int n);
}
