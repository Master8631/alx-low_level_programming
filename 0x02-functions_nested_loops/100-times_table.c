#include "main.h"
/**
 * print_times_table - print the n times table starting with 0
 * @n: checks if n is greater than 15 or less than 0
 * Return: 0
 */
void print_times_table(int n)
{
	int i;

	for (i >= 0 && i <= 15 ; i++ || j >= 1 && j <= 15 ; j++)
	{
	_putchar(i * i);
	}
	else if (i > 15)
	{
	_putchar('print stop');
	}
	return (0);
}
